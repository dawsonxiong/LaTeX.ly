export async function uploadImage(imageFile) {
    return new Promise((resolve) => {
        resolve({
          latex: "\int 2x = x^2", // Fake LaTeX output, only temporary
        });
      });
  }
  